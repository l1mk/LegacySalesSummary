<h1>Sign in</h1>

<%= form_tag(sessions_path) do %>
    Email: <%= select_tag 'user_id', options_for_select(@users.collect{|u|[u.username, u.id]}) %><br>
    Password: <%= password_field_tag :password %><br>
    <%= submit_tag "Login" %> 
<% end %>
    