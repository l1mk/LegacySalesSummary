<h1>Edit User Page</h1>
<h3><%= link_to "Delete", user_path(@user), data: { method: :delete, confirm: "Are you sure you want to delete the User?" } %></h3>

<%= form_for @user do |f| %>
  <%= render 'form', :f => f %>
<% end %>